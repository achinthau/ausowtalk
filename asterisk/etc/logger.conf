[general]
dateformat=%F %T

[logfiles]
; Everything to stdout so `docker compose logs -f asterisk` is the single place
; to look. No log files inside the container to keep the image stateless.
console => notice,warning,error,verbose(3)
